# tcs.core Config

`Module.Defaults.json` holds the default settings for every tcs module. A module can ship its
own `Config/Module.Defaults.json` to override them. User settings are stored outside the
module; see `Get-Help Get-ModuleConfig`.
